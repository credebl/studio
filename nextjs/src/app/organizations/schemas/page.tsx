import SchemaSelection from '@/features/schemas/components/SchemaSelection';
import React from 'react';

const page = () => {
  return (
    <div>
      <SchemaSelection />
    </div>
  );
};

export default page;
