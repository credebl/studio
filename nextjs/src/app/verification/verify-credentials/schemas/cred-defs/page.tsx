import CredDefSelection from '@/features/verification/components/CredDefSelection'
import React from 'react'

const page = (): React.JSX.Element => (
  <div>
    <CredDefSelection />
  </div>
)
export default page
